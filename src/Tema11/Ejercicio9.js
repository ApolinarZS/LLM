// Creamos una variable vacía
let almohadillas = "";

// Hacemos un bucle que dé 7 vueltas
for (let i = 0; i < 7; i++) {
  // En cada vuelta, le añadimos un "#" a lo que ya teníamos
  almohadillas = almohadillas + "#";

  // Y lo mostramos por consola
  console.log(almohadillas);
}